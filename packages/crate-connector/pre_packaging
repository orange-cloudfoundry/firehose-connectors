set -e

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/connectors
./mvnw -B -e clean install -DskipTests=true
mv crate-nozzle/target/crate-nozzle-0.1-SNAPSHOT.jar ${BUILD_DIR}/crate-nozzle.jar 

cd ${BUILD_DIR}
rm -rf connectors

cleanup_java

